package org.interledger.wire;

/**
 * <p>A top-level interface for all Interledger objects that can be encoded and decoded using an
 * Interledger wire format.</p>
 */
public interface InterledgerPacket {
  // Type is (likely) unnecessary in this interface because type is determined in the decoder...
}
